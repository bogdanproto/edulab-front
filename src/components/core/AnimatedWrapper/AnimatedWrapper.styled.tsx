import styled from '@emotion/styled';
import { motion } from 'framer-motion';

export const ElementsWrapper = styled(motion.div)`
  overflow: hidden;
  opacity: 0;
  margin-top: 8px;
  transition: all ease;
`;
